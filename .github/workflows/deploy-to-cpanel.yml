name: Deploy Next.js to cPanel

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: |
        npm ci
        # Install TypeScript types if missing
        npm install --save-dev @types/node @types/react @types/react-dom || echo "TypeScript types installed or already present"

    - name: Build Next.js project
      run: npm run build

    - name: List build output (for debugging)
      run: ls -la ./out/

    - name: Deploy to cPanel via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.faef.se
        username: faefse
        password: l(S9S8D(Yti9o3
        port: 21
        server-dir: /public_html/
        local-dir: ./out/
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.env*
          **/.next/
          .github/
          *.yml
          README.md
        dangerous-clean-slate: false

    - name: Verify deployment
      run: echo "🎉 Next.js project successfully deployed to cPanel!"