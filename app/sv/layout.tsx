import type { Metadata,Viewport } from 'next';import '../globals.css';import Header from '@/components/HeaderSv';import Footer from '@/components/FooterSv';export const viewport:Viewport={width:'device-width',initialScale:1,maximumScale:1};export const metadata:Metadata={title:'ViTech Cloud – Hanterade IT & moln',description:'Svenska managed IT-tjänster, open-source och molnimplementation.'};export default function Layout({children}:{children:React.ReactNode}){return(<html lang='sv'><body className='min-h-screen flex flex-col antialiased'><Header/><main className='flex-1'>{children}</main><Footer/></body></html>)}