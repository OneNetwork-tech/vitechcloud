import type { Metadata,Viewport } from 'next';import '../globals.css';import Header from '@/components/HeaderEn';import Footer from '@/components/FooterEn';export const viewport:Viewport={width:'device-width',initialScale:1,maximumScale:1};export const metadata:Metadata={title:'ViTech Cloud – Managed IT & Cloud',description:'Managed IT, open-source systems and cloud deployments.'};export default function Layout({children}:{children:React.ReactNode}){return(<html lang='en'><body className='min-h-screen flex flex-col antialiased'><Header/><main className='flex-1'>{children}</main><Footer/></body></html>)}